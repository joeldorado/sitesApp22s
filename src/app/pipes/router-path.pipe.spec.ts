import { RouterPathPipe } from './router-path.pipe';

describe('RouterPathPipe', () => {
  it('create an instance', () => {
    const pipe = new RouterPathPipe();
    expect(pipe).toBeTruthy();
  });
});
